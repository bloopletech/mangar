<!DOCTYPE html>
<html xmlns="">
  <head>
    <title><%= @title ? "#{@title}: " : "" %>Manga Reader</title>
    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon" /> 
    <%= stylesheet_link_tag %w(style lightwindow formtastic formtastic_changes) %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <%= yield :head %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
