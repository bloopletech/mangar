<!DOCTYPE html>
<html xmlns="">
  <head>
  <title><%= @title ? "#{@title}: " : "" %>Manga Reader</title>
    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon" /> 
    <%= stylesheet_link_tag %w(style pages) %>
    <%= javascript_include_tag :defaults, 'pages' %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
