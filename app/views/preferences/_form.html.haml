= simple_form_for(@preference, html: { class: 'form-horizontal' }) do |f|
  = f.input :name, as: :string
  = f.input :value, as: :text, input_html: { class: 'input-xxlarge' }
  .form-actions
    = f.button :submit, class: 'btn btn-primary'